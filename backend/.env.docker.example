# Docker Compose Environment Variables (Example)
# This file is used for local Docker testing with docker compose
# For ROFL deployment, use "oasis rofl secret set" instead

# ⚠️  INSTRUCTIONS:
# 1. Copy this file: cp .env.docker.example .env.docker.local
# 2. Replace placeholder values with your actual keys
# 3. Never commit .env.docker.local to git!

# Blockchain - Zircuit Network
ZIRCUIT_RPC_URL=https://zircuit-garfield.liquify.com
CHAIN_ID=48898

# Deployed Contracts on Zircuit Testnet
INVOICE_TOKEN_ADDRESS=0x0b627dC0ddeD44149b9e5Fb78C43E693de7CB717
POOL_ADDRESS=0x187a5B23B5552fEFA81D1AFe68626A84694F3510

# Relayer (⚠️  REPLACE with your actual private key!)
RELAYER_PRIVATE_KEY=0xYOUR_RELAYER_PRIVATE_KEY_HERE
RELAYER_ADDRESS=0xYOUR_RELAYER_ADDRESS_HERE

# Oracle (⚠️  REPLACE with your actual private key!)
ORACLE_PRIVATE_KEY=0xYOUR_ORACLE_PRIVATE_KEY_HERE
ORACLE_ADDRESS=0xYOUR_ORACLE_ADDRESS_HERE

# Government API
GOV_INVOICE_API_URL=https://api.einvoice.nat.gov.tw
GOV_API_KEY=your_api_key

# Event Listener (disable for testing without RPC subscription support)
ENABLE_EVENT_LISTENER=false

# Monitoring (optional)
ALERT_WEBHOOK_URL=
MIN_RELAYER_BALANCE=0.01
