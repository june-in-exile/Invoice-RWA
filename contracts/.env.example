# ==============================================
# Environment Variables for Smart Contract Deployment
# ==============================================
#
# How to use:
# 1. Copy this file to .env (i.e., `cp .env.example .env`)
# 2. Fill in the address for each role.
# 3. This file is loaded by `foundry.toml` and used by deployment scripts.

# Role Addresses
# ----------------------------------------------

# The address that will have the highest level of administrative control over the contracts.
# Can grant/revoke roles and manage system-wide settings.
ADMIN_ADDRESS=0xYOUR_ADMIN_ADDRESS

# The address for the backend's minter wallet (likely the relayer).
# This role is responsible for minting all InvoiceToken NFTs.
MINTER_ADDRESS=0xYOUR_MINTER_ADDRESS

# The address for the backend's oracle wallet.
# This role is responsible for fetching lottery results and reporting them to the Pool contract.
ORACLE_ADDRESS=0xYOUR_ORACLE_ADDRESS

# The address for the backend's claim operator wallet (likely the relayer).
# This role is responsible for calculating rewards and distributing them to users.
CLAIM_OPERATOR_ADDRESS=0xYOUR_CLAIM_OPERATOR_ADDRESS


# Initial Pool Configuration
# ----------------------------------------------

# The beneficiary address for the first sample pool to be registered during deployment.
BENEFICIARY_ADDRESS=0xYOUR_BENEFICIARY_ADDRESS
